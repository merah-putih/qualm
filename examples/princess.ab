abds([fight/1]).

minion(spider).
minion(ninja).

human(ninja).

false <- know_gandhi, not follow_gandhi.
follow_gandhi <- minion(X), human(X), not fight(X).


know_gandhi.

% Alternatively, we could consider a program consisting only:
% follow_gandhi <- minion(X), human(X), not fight(X).
